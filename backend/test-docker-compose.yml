version: "3.9"
services:
    postgres:
        image: postgres:latest
        container_name: test-db
        hostname: postgres
        ports:
            - "5432:5432"
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: test
volumes:
    test_data:
